pipeline {
    agent any

    environment {
       
        IMAGE_NAME = "simple-app"
    }

    stages {

        stage('Build docker image') {
            steps {
                sh 'docker-compose build  .'
            }
        }

        stage('Run container') {
            steps {
                sh '''
                  docker-compose up -d 
                '''
            }
        }
    }
}
